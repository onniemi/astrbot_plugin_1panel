{
  "panel_host": {
    "type": "string",
    "default": "http://localhost:10086",
    "description": "1Panel 面板地址（包含端口，如 http://192.168.1.1:10086）"
  },
  "panel_api_key": {
    "type": "string",
    "default": "",
    "description": "1Panel API 密钥（在面板设置 -> 面板 -> API 接口中获取）"
  },
  "verify_ssl": {
    "type": "bool",
    "default": false,
    "description": "是否验证 SSL 证书（自签名证书请设为 false）"
  },
  "enable_whitelist": {
    "type": "bool",
    "default": false,
    "description": "是否启用白名单权限管理（启用后仅白名单用户可使用）"
  },
  "whitelist_users": {
    "type": "list",
    "default": [],
    "description": "白名单用户ID列表（使用 /panel whoami 查看用户ID）"
  }
}
